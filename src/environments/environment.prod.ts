export const environment = {
  production: true,
  isServer: true,
  // for prerender
  host: 'http://localhost:4000',
  apiBaseUrl: 'https://backend.snow.com/',
  apiBaseUrlBlog: 'https://leadsbe.pingcall.com/',
};
