export const environment = {
  production: true,
  isServer: true,
  // for prerender
  host: 'http://localhost:5000',
  apiBaseUrl: 'https://backend.kodehash.com/',
  apiBaseUrlBlog: 'https://leadsbe.pingcall.com/',
};
